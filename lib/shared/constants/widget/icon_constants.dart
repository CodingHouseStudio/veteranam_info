import 'package:flutter/material.dart';

abstract class KIcon {
  static const Icon micIcon = Icon(Icons.mic_none_outlined);
  static const Icon personIcon = Icon(Icons.person_outline);
  static const Icon search = Icon(Icons.search);
}
