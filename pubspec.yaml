name: kozak
description: VeteranUA
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  bloc: ^8.1.3
  cached_network_image: ^3.3.1
  easy_debounce: ^2.0.3
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.4
  flutter_localizations:
    sdk: flutter
  font_awesome_flutter: ^10.7.0
  get_it: ^7.6.0 # Used for dependency injection
  go_router: ^13.2.0 # Decoupled routing package which is a must for any app
  injectable: ^2.3.4 # Used for dependency injection

  intl: ^0.18.1
  shared_preferences: ^2.2.2
  web: 0.5.1
dev_dependencies:
  bloc_test: ^9.1.7
  build_runner: ^2.4.8
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.5.0 # Used for dependency injection for get_it
  integration_test:
    sdk: flutter
  intl_utils: ^2.8.7
  mockito: ^5.4.4 # Used for mocking classes
  mocktail: ^1.0.3
  very_good_analysis: ^5.1.0

flutter:
  uses-material-design: true
  generate: true
